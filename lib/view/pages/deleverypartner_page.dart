import 'package:flutter/material.dart';

class DeliveryPartnerPage extends StatelessWidget {
  const DeliveryPartnerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('DeliveryPartner'),),
    );
  }
}